# {项目全称}｜项目基线索引

> 最后更新：YYYY-MM-DD ｜ 原则：只收录跨版本稳定、已确认的可靠输入。

## 目录导航

| 目录/文件 | 用途 | 状态 |
|---|---|---|
| `关键决策.md` | 影响后续设计的已确认决策，编号从 #D001 自增 | 已初始化 |
| `需求池.md` | 尚未进入具体版本的候选需求 | 已初始化 |
| `业务规则/` | 跨版本稳定的业务计算、口径和边界 | 按需创建 |
| `设计规范/` | 品牌、组件、交互和原型规范 | 按需创建 |
| `架构图/` | 系统、业务与页面流转图 | 按需创建 |
| `法律协议/` | 合规、合同与协议约束 | 按需创建 |
| `会议记录/` | 外部输入的原始纪要与时间线 | 已初始化 |
| `项目工具/` | 仅本项目使用的流程说明、模板与检查项 | 已初始化 |
| `_source/` | 基线相关的二进制原件及转换稿 | 按需创建 |

## 维护规则

1. 未确认、一次性或仅本版本有效的内容不进入项目基线。
2. 进入具体版本的需求、规格、原型与 TAPD 规划仍放在 `02-迭代/` 对应版本下。
3. 每次新增基线资产后，更新本索引和关联的关键决策或会议时间线。

## 最近更新

| 日期 | 更新内容 | 关联位置 |
|---|---|---|
| YYYY-MM-DD | 初始化项目基线 | `01-项目基线/` |
