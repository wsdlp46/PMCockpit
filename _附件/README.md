# _附件

> 二进制文件归档区：图片、截图、PDF、Word、Excel 等无法被 AI 直接读取的文件。

## 用途

编辑文档时粘贴的图片、收到的 PDF 原件、客户发的 Excel 等，都放这里。
不要直接放项目目录里，会污染 AI 的上下文。

## 与 _source 的区别

- `_附件/`：零散的、未整理的二进制文件（截图、临时文件）
- 项目内的 `_source/`：已归入某项目的二进制原件 + 转换后的 MD

> 如果文件重要且需要 AI 读取，转成 MD 后放进对应项目的 `_source/`。
