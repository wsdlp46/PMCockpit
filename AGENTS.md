# AGENTS.md

> 工作区级常驻内核。完整流程由 `01通用规则/项目工作流规范.md` 路由到按需细则和 Skill；不得把细则全文复制回本文件。

## 工作区定位与权威源

本工作区承载 B 端和政务数据产品的需求、规格、原型、测试、交付、复盘与经验沉淀。

- 通用规则以 `01通用规则/` 为权威，Skill 以 `.ai/skills/` 为唯一权威源。
- ZCode、WorkBuddy、Codex、Claude Code 默认安装同一套 8 个核心 Skill；仅 ZCode 链接 `.ai/agents/solution-designer.md`。测试和 VitePress 仅在用户显式启用时安装；VitePress 还要求已有独立站点。
- MCP、连接器和外部操作遵循 `01通用规则/AI能力治理规范.md` 与 `00环境配置/MCP能力清单.md`。
- 工具记忆、缓存、日志和本地副本不能承载关键决策或通用规则。

## 最小上下文与任务路由

进入既有项目先读项目 `00-项目总览.md` 和 `02-迭代/_index.md`。涉及需求、范围或设计判断时再读 `01-项目基线/关键决策.md`；涉及最新外部输入时再读最新会议记录；涉及行业、客户、角色、政策、业务模式或竞品判断时先读 `03知识库/_index_AI匹配.md`。

| 用户目标 | 必读规则与能力 |
|---|---|
| 新建项目或项目立项 | 项目模板 + `项目工作流/01-项目结构与版本治理.md`；不调用 `project-iteration` |
| 启动、收尾版本或校正索引 | 项目结构与版本治理 + `project-iteration` |
| 文件、会议纪要、二进制入库 | 材料与知识资产治理 + `project-intake-pm` |
| PRD、规格、原型、TAPD、对外文档 | 文档与页面资产规范 + 对应 Skill |
| 市场、客户、行业、政策、竞品调研 | 知识库匹配索引 + `research-pm` |
| 测试（已显式启用可选能力） | `AI能力路由规范.md` + `test-flow-pm` |
| 复盘 | `AI能力路由规范.md` + 对应核心 Skill |
| 文档站（已显式启用可选能力且已有站点） | `AI能力路由规范.md` + `vitepress-deploy` |
| 多个能力同时命中 | `AI能力路由规范.md` 的核心路由裁决；可选能力只在启用后参与 |

## 不可绕过的约束

1. 新建或修改需求前读取关键决策；原型前读取对应规格。
2. 原型未明确确认前，只能改原型、归档旧版、写临时变更笔记；不得同步需求、规格、文档站、TAPD 或外部系统。
3. 改原型后同步同一功能点的规格和需求；规格必须覆盖页面全部模块。
4. 材料入库保留原件，优先读取转换产物；归属不明确先确认项目和版本。
5. 内部材料使用 `#D001` 追溯关键决策；对外材料不得暴露内部决策编码或“决策依据”。
6. 外部写入前确认目标、范围、数量；删除、覆盖、归档和远端清理必须列精确目标并二次确认。
7. 调研先确认主题、场景假设、报告用途和决策定位；绝对化结论先搜反例。
8. 产出文件功能先确认受众、用途和形态；默认推荐在线预览、打印或截图等轻量方案。
9. 项目名称首次使用全称；项目阶段完成、新增删除文件或重大变更后更新真实索引。
10. 交付前检查编号、引用、链接、重复残留和受众边界；问题发现即修复。

## 输出底线

- 对内材料保持完整、可追溯、角色边界清晰、流程有触发与结果。
- 对外材料只表达客户可感知能力，不披露内部运营、财务、分销、未授权合作方或内部决策编码。
- 客户级正式文档使用全中文专业表达，不使用箭头、等号或 emoji；研发交底按页面或模块，用白话说明字段、按钮、状态和结果。
- 任务收尾先列产出物和路径，再说明结果；生成或编辑文档时读取《文档与页面资产规范》完成详细自查。
